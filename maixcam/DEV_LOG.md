# MaixCAM 端开发记录（v2 · 纯打印测试版）

> v1（接 STM32 串口协议版）已备份到 `backup/`，见 `backup/说明_v1_备份.md`。
> 本文件记录 v2 的需求确认（问答）与开发过程。

## 背景与目标（2026-09-17）

* 现状：**MaixCAM 只能通过 USB 连电脑**（用 MaixVision 在线运行），
  与 STM32 的串口还没接（计划 A16/A17 ↔ PA2/PA3）。
* 所以 v2 的目标是：**先在 MaixCAM 端自己把视觉调通**，
  用"打印 + 屏幕显示"的方式把识别到的信息（小球/目标的位置、尺寸、像素数、耗时等）呈现出来，
  方便对着真实物体标定阈值、看算法效果。
* 暂时**不需要**：UART 协议、STM32 交互、被动等待 `0x12`。

## 待确认问题清单（逐条问答，答一条记一条）

| # | 问题 | 结论 |
| :- | :--- | :--- |
| 1 | 在 MaixCAM 上先测什么目标？（排爆物小球 / 白色人质立体形状 / 只要颜色 / 全部） | ⏳ 待确认 |
| 2 | "小球信息"具体想看哪些字段？（编号、坐标 x/y/w/h、中心 cx/cy、像素数、外接圆、耗时…） | ⏳ 待确认 |
| 3 | 打印到哪里？（MaixVision 终端 / 屏幕 overlay 画框+文字 / 两者都要） | ⏳ 待确认 |
| 4 | 相机到物体的距离与安装（10cm 固定 / 手持 / 机械臂上）？分辨率用 640×320 还是别的 | ⏳ 待确认 |
| 5 | 是否要一个"实时 LAB 值/点击取阈值"的标定模式，方便现场快速卡阈值？ | ⏳ 待确认 |
| 6 | 物体是一次只出现一个（按赛题），还是要同时检测多个并排序输出？ | ⏳ 待确认 |
| 7 | 打印频率/循环方式：连续识别循环（方便观察）还是按键/定时触发一次？ | ⏳ 待确认 |
| 8 | v2 是否要保留 v1 的协议代码（方便以后一键切回联调），还是完全独立？ | ⏳ 待确认 |
| 9 | 白色形状部分要不要顺便做"轮廓/剖面曲线"打印（帮助判断 50px 阈值该怎么定）？ | ⏳ 待确认 |
| 10 | 现场灯光下的容错：是否需要多阈值(如红色两组)合并、形态学去噪、ROI 限定？ | ⏳ 待确认 |

## 开发过程记录

（等需求确认后逐条补）

## 运行方式（预期）

1. USB 线连 MaixCAM 与电脑，打开 MaixVision。
2. 打开 v2 脚本，点运行 → 代码推送到设备执行。
3. MaixVision 的终端窗口看打印；MaixCAM 屏幕看识别框与文字。
